[package]
name = "contract_a1"
type = "contract"
compiler_version = ">=1.0.0"
authors = [""]

[dependencies]
aztec = { git = "https://github.com/AztecProtocol/aztec-packages/", tag = "v4.0.0-devnet.2-patch.1", directory = "noir-projects/aztec-nr/aztec" }
# aliased as `contract_b` so the Noir source is identical to contract_a2
contract_b = { path = "../contract_b_v1" }
