name: Main Tests

on:
  push:
    branches:
      - main

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  tests:
    uses: defi-wonderland/aztec-ci-actions/.github/workflows/main-tests.yml@feat/cache-aztec
    secrets: inherit
